# SENSTA iOS 프로젝트 상태

## 현재 목표

- SENSTA Android의 제품 동작과 GOAPI API contract v1을 기준으로 iPhone용 네이티브 SwiftUI 앱을
  개발한다.
- 승인된 공개 피드·탐색·상세·사진가 화면을 유지하며 로그인 기반 참여 기능을 추가한다.
- 이메일 로그인·Keychain 세션 복원·토큰 갱신·로그아웃, 게시글·댓글 좋아요와 댓글 작성·답글·본인
  댓글 수정·삭제를 구현했다. Google 로그인 클라이언트와 서버 검증 강화를 구현하고 실제 OAuth 로그인을
  확인했다. Apple 로그인과 기존 계정의 명시적 연결 계약도 구현했고, 첫 운영 QA에서 실패 이유를 바로
  확인할 수 있도록 오류 안내와 서버 진단을 보강했다. 이메일 인증 회원가입도 기존 GOAPI 계약으로
  연결했다. 첫 피드에는 플랫폼 공통 Sensta 앱 업적, 인앱 알림과 GPS를 제거하는 다중 사진 업로드를
  연결했다.

## 현재 단계

- iOS 17 이상·iPhone 전용 SwiftUI 앱과 unit/UI test target, shared scheme을 만들고 첫 공개 사진 피드를
  구현했다.
- `GET /board/list?id=photo&page=1&option=0&keyword=` 운영 응답을 Android DTO와 대조했다. fixture 기반
  Swift DTO decoding, 오류 envelope, 공개 요청과 차단 목록 필터 회귀 테스트를 갖췄으며, 로딩·빈 상태·
  오류·재시도·당겨서 새로고침을 제공한다.
- 목록 `cover` 상대 경로는 Android와 같이 `t*.webp`에서 `f*.webp` 미리보기로 바꾸고 `sensta.me`의
  HTTPS 절대 URL로 해석한다. 작성자, 앱 포토그래퍼 업적, 제목과 좋아요·댓글 수를 네이티브 오버레이로
  표시하며 조회수와 등록일은 첫 피드에서 생략한다.
- Android의 대표 피드 정체성을 이어받아 사진 한 장이 노치·홈 인디케이터 영역을 포함한 화면 전체를
  채우고 세로 페이지 단위로 스냅되도록 구성했다. 좌상단 워드마크는 Android가 사용하는 Oleo Script
  Bold 원본의 `SENSTA` 글자 윤곽·크기·배치·투명도를 그대로 옮긴 벡터이며, 하단 그라데이션 위에는
  작성자·제목·통계를 배치한다. UI test가 카드의 화면 네 변과 워드마크 이미지 노출을 검증한다.
- 피드 카드를 누르면 익명 `GET /board/view?id=photo&postUid=...&needUpdateHit=0&latestLimit=5` 요청으로
  게시글 상세를 연다. 여러 사진을 가로로 넘기고 각 사진의 EXIF·AI 설명, 본문·태그·첨부 파일 정보·통계와
  시스템 공유를 표시한다. 상세 사진도 화면 양쪽 16pt 안에 고정하는 UI 회귀 테스트를 갖췄다.
- Apple ImageIO가 2400px 고화질 미리보기를 원본·표시 영역 종횡비와 화면 scale에 맞춰 백그라운드에서
  다운샘플링한다. 디코딩 메모리 캐시와 응답 디스크 캐시, 동일 요청 병합·마지막 소비자 취소, 피드 다음
  사진과 상세 앞·뒤 사진 예열을 적용했다. 상세 사진은 손가락 위치를 연속 추적하는 가로 ScrollView
  paging으로 바꿨다.
- macOS 27 beta 환경에서 `/Applications/Xcode-beta.app`의 Xcode 27.0(`27A5252f`), Swift 6.4,
  iOS 27.0 SDK와 시뮬레이터를 확인했다.
- 실제 iPhone은 준비되어 있다.
- 약 10년 전 만료됐던 Apple Developer Program 재가입이 2026-09-04 승인됐다. Team ID는
  `WKPCU58CWL`이며 멤버십은 2027-09-04까지 활성이다. Apple Developer 계약과 App Store Connect
  이용 약관도 동의했다.
- Xcode 라이선스·초기 구성과 Apple Account Team `WKPCU58CWL` 연결을 마쳤다. 실제 iPhone에서 개발자
  모드를 켜고 자동 서명으로 기기 등록, Apple Development 인증서와 `me.sensta.ios.debug` provisioning
  profile 생성, 앱 설치·실행까지 확인했다.
- 계정의 2011년 레거시 기기 3대를 제거하고 연간 등록 슬롯을 초기화했다. 기존 SENSTA App ID나 App
  Store Connect 앱 레코드는 없었다.
- 운영 `me.sensta.ios`와 개발 `me.sensta.ios.debug` App ID를 만들고 Associated Domains, Push
  Notifications, Sign in with Apple capability를 활성화했다.
- App Store Connect에 `SENSTA` 앱(Apple ID `6808687447`, SKU `sensta-ios`)을 생성했다. 가격은 무료,
  배포 국가는 대한민국, 주 카테고리는 사진 및 비디오, 보조 카테고리는 소셜 네트워킹, 출시는 수동으로
  설정했다. 검증 전인 Apple Silicon Mac과 Apple Vision Pro 배포는 껐다. 개인 개발자 계정에 필요한
  대한민국 연락처 및 사업자등록번호 보유 여부 확인도 완료했다.
- Debug는 `me.sensta.ios.debug`, Release는 `me.sensta.ios`를 사용하고 두 구성 모두 운영
  `https://sensta.me/goapi/`를 Info.plist에 주입한다. Associated Domains, Push Notifications와 Sign in
  with Apple entitlement도 App ID 설정과 맞췄다.
- Xcode 27의 iPhone 17 Pro 시뮬레이터에서 Debug test build, Release build와 정적 분석을 통과했다. API
  설정·피드/상세 계약·상태·이미지 파이프라인 unit test 18개와 launch·전체 화면 피드·상세 이동/폭 UI test
  3개가 통과했으며 운영
  데이터 피드로 라이트·다크 모드와 큰 글자 레이아웃을 확인했다.
- iPhone 17 실기기용 Debug 빌드를 Apple Development 인증서와 Team `WKPCU58CWL`로 서명했고,
  공개 피드와 게시글 상세가 포함된 `me.sensta.ios.debug` 설치·실행을 확인했다. 사진 감상 리뷰용 빌드는
  Swift 최적화 `-O`를 임시 적용해 설치했다.

## 결정

- Android 저장소와 분리된 `sirini/sensta-ios` 공개 저장소에서 개발한다.
- SwiftUI와 Swift concurrency를 사용하며 Apple 기본 프레임워크를 우선한다.
- Android와 동일한 백엔드 데이터를 사용하되 iOS 관례에 맞는 사용자 경험을 설계한다.
- 현재 개발에는 Xcode 27 beta를 사용하고, TestFlight·App Store 제출 전 당시 Apple이 허용하는 안정판
  Xcode와 SDK로 다시 검증한다.
- 시스템 전체 Xcode 선택을 강제로 바꾸지 않고 저장소 스크립트가 `DEVELOPER_DIR`를 지정한다.
- 운영 bundle ID는 `me.sensta.ios`, 개발·QA bundle ID는 `me.sensta.ios.debug`로 확정한다.
- 최소 지원 버전은 iOS 17이며 초기 배포 대상은 iPhone이다. iPad는 별도 QA 뒤 확대한다.
- access/refresh token은 Keychain에 저장하고 민감하지 않은 설정만 UserDefaults에 둔다.
- 정확한 GPS 위치는 공개 업로드에 포함하지 않는다.
- 공개 피드는 로그인 없이 직접 GOAPI를 호출한다. 요청에는 `Authorization`을 붙이지 않으며 서버가 주는
  차단 목록 필터는 Android와 동일하게 적용한다.
- 사진 감상 품질과 스크롤·페이징의 부드러움을 제품 우선순위로 둔다. 저해상도 파일로 바꾸지 않고 표시
  픽셀에 필요한 고화질 데이터를 보존한 채 다운샘플링·캐시·예열로 메모리와 프레임 시간을 관리한다.
- Android와 공유하는 Oleo Script Bold 워드마크는 글꼴 파일을 런타임에 중복 탑재하지 않고 동일 glyph
  outline의 벡터 asset으로 유지하며 SIL Open Font License를 저장소에 보존한다.
- 앱 기능은 fixture와 요청 계약 테스트를 먼저 만든 뒤 작은 수직 기능 단위로 구현·검증한다.

## 선행 조건

- iOS 인증·푸시·앱 출처·HEIC 및 UGC 심사 대응을 위한 GOAPI 사전 작업
- Firebase iOS 앱, Google OAuth iOS client, APNs 인증 키 준비
- App Store 지원 페이지와 개인정보·심사 메타데이터 준비

## 2026-09-05 리뷰와 오늘 작업

- 새벽 마지막 커밋 `641b733`까지 공개 피드·상세·이미지 파이프라인과 Android/GOAPI 계약을 리뷰했다.
- 요청 취소가 `URLError.cancelled`로 전달되면 일반 통신 오류로 바뀌던 문제를 수정했다. 취소된 응답은
  피드·상세 상태에 반영하지 않는다.
- 새로고침 실패 시 기존 사진 목록을 유지하고 별도 오류 안내·재시도를 제공한다.
- 첫 수정은 단위 테스트 21개·UI 테스트 3개와 Debug test build·Release simulator build를 통과했다.
- 공개 피드 마지막 세 장에 가까워지면 다음 페이지를 불러온다. GOAPI의 `rowCount - notices.count`와
  필터 전 글 수·전체 글 수로 종료를 판단하고, 중복 ID를 제거하며 차단/중복만 있는 페이지를 건너뛴다.
- 추가 로딩 실패는 현재 사진과 위치를 유지한 채 마지막 사진 위에서 수동 재시도를 제공한다. 새로고침은
  진행 중인 추가 요청을 취소하며 이전 응답이 새 목록에 섞이지 않도록 요청 세대를 구분한다.
- 제품 소유자가 어제 최종 실기기 빌드에서 버벅임·멈칫함 없이 부드럽게 사진 감상에 집중할 수 있었고,
  Android 대비 세련된 디자인에도 만족한다고 확인했다. 기존 감상 경험을 후속 기능의 기준으로 유지한다.
- 최종 단위 테스트 27개·UI 테스트 4개와 Debug test build·Release simulator build를 통과했다.
  Debug 전용 고정 데이터로 페이지 추가 실패·재시도·기존 위치 유지·다음 페이지 스와이프를 검증한다.
- 오늘의 복구 수정·페이지 로딩·자동 검증은 완료했다. 로그인은 공용 mobile 인증 계약 정리가 선행되는
  다음 독립 작업이다. 오늘 변경분의 실기기 QA는 아직 수행하지 않았다.

## iOS 우선 후속 개발

- 제품 소유자 요청에 따라 서버 변경 없이 가능한 iOS 작업을 먼저 진행한다. Android 운영 API의 경로·
  요청·응답을 유지하고 인증·푸시 서버 변경은 필요한 시점에 최소 범위로 별도 검증한다.
- 첫 피드는 기존 정체성을 유지한다. 나머지 화면은 사진 전체 구도·여백·절제된 정보 배치와 iOS 기본
  조작을 우선한다.
- 상세 사진을 잘라 채우는 방식에서 전체 구도 표시로 바꾸고, 검은 배경의 전체 화면 감상·핀치/두 번 탭
  확대·사진 넘기기·닫기를 추가했다. VoiceOver 확대 액션과 동작 줄이기도 지원한다.
- 상세 촬영 정보·설명을 처음에는 독립적으로 배치했으나 제품 소유자 피드백에 따라 Android와 같은
  단일 패널 구조로 개선했다. 상단은 간결한 EXIF 요약, 하단은 AI 설명이며 시스템의 라이트/다크 색을 따른다. 단위 28개·UI 5개와 Debug/Release
  빌드를 통과했고, 테스트 캡처에서 상세와 전체 화면 배치를 확인했다. 확대 감상의 실기기 QA는 남아 있다.
- 댓글은 상세 본문 아래에서 스와이프로 바로 읽는다. 우상단 버튼은 댓글 위치로 이동하며, 기존
  `GET /comment/list`를 익명 호출한다. 상세 응답의 게시판 UID를 사용하고
  공개 댓글·답글·좋아요 수·날짜, 더 보기와 오류 재시도를 표시한다. 비공개/알 수 없는 status는 표시하지
  않으며 게시판·게시글 불일치 응답도 거부한다.
- 댓글 포함 최종 단위 36개·UI 6개와 Debug/Release 빌드가 통과했다. 운영 댓글 API의 success 응답과
  스크린샷 배치를 확인했다. 기존과 같은 임시 Swift `-O` 설정의 서명 Debug 빌드를 연결된 iPhone 17에
  설치·실행했다. 핀치·상세 왕복·댓글의 실기기 제품 QA는 제품 소유자가 진행한다.
- GOAPI·Android 코드는 변경하지 않았다. 서버 업데이트·재시작과 Firebase 설정은 이번 작업에 필요 없다.

- EXIF/AI 테마 패널과 본문 아래 댓글 배치를 추가 검증했다. 단위 36개·라이트/다크/큰 글자 UI와 댓글
  스와이프·페이지 추가·재시도, Debug/Release 빌드가 통과했고 캡처에서 패널 배치를 확인했다.

- 공개 제목 검색은 검색어별 목록 상태와 추가 페이지·빈 결과·재시도·새로고침을 제공한다. 사진 격자는
  기본 2열, 접근성 큰 글자에서 1열이다. 서버의 추가 QueryUnescape에 맞춰 한글·특수문자 검색어를
  인코딩하며 운영 `커피` 검색과 `% + &` 검색의 success 응답을 확인했다.

- 검색 포함 최종 단위 37개·UI 8개와 Debug/Release 빌드가 통과했다. 검색 격자·상세 이동과 EXIF/AI
  라이트/다크/큰 글자 캡처를 확인했다. 최종 서명 Debug 앱은 기존 감상 QA와 같은 Swift `-O` 설정으로
  빌드했다.

## 다음 작업

1. 실제 iPhone에서 새 하단 배치와 운영 알림의 미확인·읽음 처리, JPEG·HEIC 다중 업로드를 확인한다.
2. Android 계약을 기준으로 이메일 비밀번호 재설정과 내 작품 스튜디오를 구현한다.
3. 미확인 영구 업적의 iOS 축하 화면과 확인 처리를 연결한다.
4. 1:1 대화와 iOS push 등록, APNs/FCM 알림·딥 링크를 구현한다.
5. 신고·차단·계정 삭제를 완성하고 App Store UGC 심사 시나리오를 검증한다.

## 탐색과 다중 검색 (2026-09-05)

- 탐색 진입 시 최근 사진을 2열 그리드로 표시하며 접근성 큰 글자에서는 1열로 전환한다.
- 기본 검색은 저장된 AI 설명(option=12), 나머지는 제목(0)·본문(1)·닉네임(2)·해시태그(3)다.
  기존 Android/GOAPI 계약을 그대로 사용하며 새 OpenAI 호출이나 의미 기반 검색을 추가하지 않는다.
- 최근 태그는 목록 응답의 config.uid로 `/board/tag/recent`를 호출한다. 태그 선택 즉시 검색하며
  태그 오류는 사진 그리드를 가리지 않고 따로 재시도한다. 검색어를 지우면 최근 사진으로 돌아간다.
- 닉네임 검색은 기존 서버의 부분 일치 사용자 한 명 선택 규칙을 따른다. 여러 사진가 통합 검색으로
  확장하지 않았으며 정확한 닉네임을 권장한다. 서버·Android 변경 및 운영 재시작은 필요 없다.
- 단위 테스트 41개·UI 테스트 9개와 Debug/Release 빌드가 통과했다. 운영 API의 5개 검색 옵션과 최근
  태그 success 응답, 테스트 캡처의 그리드·태그·선택 상태를 확인했다. 기존 Swift `-O` 서명 빌드로
  iPhone 17 설치를 완료했으며 기기 잠금으로 자동 실행은 하지 못했다. 실기기 제품 QA는 남아 있다.

## 사진가 화면 (2026-09-05)

- 제품 소유자가 탐색의 정갈한 디자인과 사진 중심 동작을 승인했다. 다음 단계로 공개 사진가 화면을 추가했다.
- 상세 작성자 영역을 누르면 프로필 사진·닉네임·소개와 최근 작품 그리드를 표시한다. 작품을 누르면 상세로
  이동하며 돌아왔을 때 프로필을 다시 요청하지 않는다. 큰 글자는 1열, 기본은 2열이다.
- 기존 `/auth/user/info?targetUserUid=...`와 `/board/user/latest?targetUserUid=...&limit=12`를 익명으로
  사용한다. 최근 활동 12건 중 photo 게시판만 포함하므로 전체 작품 페이지네이션은 아직 제공하지 않는다.
- 활동 응답에는 썸네일이 없어 기존 상세 API를 최대 3건씩 병렬 호출한다. 공개 접근 검사와 게시글·작성자
  UID 대조를 통과한 작품만 표시하며 일부 실패 시 성공한 작품을 유지하고 재시도한다. 차단된 프로필과
  요청 UID가 다른 프로필은 표시하지 않는다. 새로고침 실패도 기존 프로필·작품을 유지한다.
- GOAPI·Android 변경과 Firebase 설정은 필요 없다. UID 기반 전체 작품 목록 API는 이후 서버 작업으로 남긴다.
- 단위 46개와 기존 UI 9개를 통과했다. 새 사진가 UI 테스트에서 작성자 행의 빈 영역 터치 누락을 발견해
  contentShape로 수정했으며 재실행에서 오류 재시도·작품 상세 진입·뒤로 가기를 통과했다. Debug/Release
  빌드와 프로필 캡처를 확인하고 수정된 Swift `-O` 서명 앱을 iPhone 17에 설치·실행했다.

## 공개 업적·활동 통계 (2026-09-05)

- 공개 프로필 응답의 업적 전체를 카드로 표시하고, 선택 시 이름·설명·획득일을 시트로 보여준다.
  임의 등급·순위를 만들지 않고 실제 획득 업적만 사용한다.
- 프로필 아래에 작품·사진·받은 좋아요를 집계한다. Android 본인 스튜디오의 집계식을 재사용하되 다른
  사용자에게는 공개 작품만 포함한다. 비공개·삭제 작품은 제외하므로 본인 통계와 다를 수 있다.
- 기존 서버에는 공개 통계가 없어 GOAPI `934d836`에 `/board/user/summary`를 추가했다. 기존 Android
  인증 API와 스키마는 유지한다. Go 전체 테스트·vet, 권한/UID/차단/공개 상태 회귀 테스트가 통과했다.
- 서버 업데이트 전 또는 통계 오류 시 `—`와 재시도를 표시하고 업적·최근 작품은 유지한다. 통계에
  최근 12개 작품의 일부 합계를 사용하지 않는다.
- 운영자는 GOAPI 저장소 최신 main을 받고 기존 배포 방식으로 재빌드·재시작한다. DB 마이그레이션은 없다.
  `https://sensta.me/goapi/board/user/summary?id=photo&targetUserUid=1`의 success=true로 확인한다.
- iOS 단위 48개·UI 10개와 Debug/Release 빌드가 통과했다. 업적 상세·통계 수치·작품 왕복 캡처를 확인했다.
  서명 Debug 앱을 iPhone 17에 설치했으며 기기 잠금으로 자동 실행은 되지 않았다. 운영 통계 확인은 서버
  업데이트 후 진행하기로 했고, 이후 제품 소유자가 운영 반영과 실제 앱 통계 동작을 확인했다.

## 세션 마무리 — 사진가 화면 정렬 (2026-09-05)

- 제품 소유자가 GOAPI `934d836`의 sensta.me 반영 및 실제 앱의 업적·통계 동작을 확인했다.
- 작품·사진·받은 좋아요의 숫자와 라벨을 각 열 가운데에 맞췄다. 업적 카드의 아이콘 높이와 두 줄 제목
  공간을 통일해 한 줄·두 줄 제목의 카드 높이를 같게 했다. 글자 크기는 Dynamic Type을 따르며,
  긴 업적명 전체와 설명은 기존 상세 시트에서 확인한다.
- 이번 세션은 정렬 수정까지만 진행한다. 다음 세션에서 로그인 기반 좋아요·댓글 참여 흐름을 시작한다.
  Firebase/OAuth 설정이 필요하면 제품 소유자에게 브라우저 작업을 안내한다.
- 한 줄·두 줄 업적이 함께 있는 캡처에서 동일한 높이와 통계 중앙 정렬을 확인했다. 관련 UI 테스트와
  Debug/Release 빌드를 통과했고, 수정된 서명 앱을 iPhone에 설치·실행했다. 백엔드 추가 변경은 없다.

## 이메일 로그인과 Keychain 세션 (2026-09-05)

- 피드 우상단 계정 버튼과 iOS 기본 Form 시트를 추가했다. 기존 이메일·비밀번호 로그인, 내 공개
  프로필, 로그아웃을 제공하며 익명 피드·탐색·상세 동작은 유지한다.
- Android와 같은 `/auth/signin`, `/auth/load`, `/auth/android/refresh`, `/auth/logout`을 사용한다.
  GOAPI·Android·웹 코드와 API 계약은 변경하지 않았으며 백엔드 교체·Firebase 설정은 필요 없다.
- 토큰 쌍은 API URL·bundle ID별 Keychain 항목으로 저장한다. 인증 쿠키·디스크 캐시·리다이렉트를
  차단하고, 401일 때만 refresh를 시도한다. 회전된 쌍은 후속 조회 전에 교체한다. 네트워크 오류는
  저장된 세션을 보존하고, 차단·무효 계정·Keychain 실패를 로그인 성공으로 표시하지 않는다.
- 전체 단위 64개·UI 11개, Debug/Release 빌드를 통과했다. 실제 Keychain 읽기·교체·삭제, 차단 계정,
  취소·중복 요청·토큰 회전·저장 실패와 로그인 실패→재시도→성공→로그아웃을 검증했다.
- 계정 화면 캡처를 확인했고 Swift `-O` 자동 서명 Debug 앱을 연결된 iPhone 17에 설치·실행했다.
  실제 계정 로그인·재실행 복원 QA는 제품 소유자에게 남아 있다. Google·Apple 로그인과 좋아요·댓글
  참여는 이번 구현 범위에 포함하지 않는다. 계약과 QA 절차는 `docs/ACCOUNT_SESSION.md`에 정리했다.

## 계정 프로필 사진과 작은 로그인 시트 (2026-09-05)

- 제품 소유자가 앱 종료·재실행 후 로그인 세션 유지를 확인했다.
- 기존 로그인·내 정보 응답의 `profile`을 사용해 내 계정 이름 옆과 피드 우상단에 원형 프로필 사진을
  표시한다. 사진이 없거나 불러오지 못하면 인물 아이콘을 표시하고, 비로그인 상태는 진입 화살표로 구분한다.
- 계정 시트는 절반 높이와 시스템 regular material 블러로 연다. 상단 장식을 줄여 입력 공간을 확보하고
  키보드 입력·공개 프로필 탐색에서는 펼친다. 접근성 큰 글자는 처음부터 전체 높이이며, 투명도 줄이기
  설정에서는 불투명 시스템 배경을 사용한다.
- 단위 64개와 관련 UI 2개에서 프로필 경로 전달, 로그인 전후 구분·프로필 이동·로그아웃, 시트 높이와
  라이트/다크/큰 글자 배치를 확인했다. Debug/Release 빌드와 캡처 검증을 마쳤다.
- GOAPI·Android·웹 계약 변경이나 백엔드 교체는 필요 없다. 다음 기능은 로그인 기반 좋아요·댓글 참여다.
- 최종 Swift `-O` 자동 서명 Debug 앱을 연결된 iPhone 17에 설치·실행했다. 실제 프로필 사진과 블러의
  실기기 디자인 확인은 제품 소유자가 진행한다.

## 고정 방향 로그인 아이콘과 게시글 좋아요 (2026-09-05)

- `ea9bac2`에서 제품 소유자가 고른 `ipad.and.arrow.forward`로 로그인 아이콘을 바꿨다. 아이콘에만
  `layoutDirection = leftToRight`를 지정해 현지화에 따른 좌우 전환을 고정했다. 캡처·관련 UI·Release
  빌드를 확인해 main에 push했다.
- 게시글 상세의 하트는 비로그인 상태에서 로그인 시트를 열며 로그인만으로 좋아요를 자동 전송하지 않는다.
  인증된 상세 조회로 개인별 liked·count와 게시판/게시글 UID를 확인한 뒤 기존 Android의
  `PATCH /board/like` JSON 계약으로 좋아요·취소를 보낸다. 서버 성공 뒤 하트·숫자를 갱신하며
  상세·피드가 같은 세션의 수치를 공유한다.
- 중복 조작을 막고 결과 유실 시 상태를 다시 읽기 전에는 추가 변경을 보내지 않는다. 인증 요청은 설정된
  API 범위로만 전송하고 동시 401의 refresh를 합친다. 회전된 토큰 쌍은 Keychain 저장 후 한 번만
  재시도하며, 로그아웃·만료 뒤 도착한 요청/응답이 이전 세션이나 좋아요 상태를 복원하지 않는다.
- 단위 73개·UI 13개, Debug/Release 빌드와 좋아요 캡처를 확인했다. 신규 회귀는 요청 body·UID 대조,
  동시 토큰 회전·잘못된 API 목적지·refresh 거부·중복 변경·로그아웃 중 refresh·응답 유실 후 재조회를
  포함한다. UI에서는 로그인 유도 → 좋아요 → 피드 수치 → 상세 복귀 → 취소를 검증했다.
- Swift `-O` 자동 서명 앱을 연결된 iPhone 17에 설치·실행했다. 실제 계정 좋아요 제품 QA는 제품 소유자가
  수행한다. GOAPI·Android·웹 코드나 배포 변경은 없고, 댓글 작성·댓글 좋아요는 다음 작업이다.

## 댓글 작성과 답글 (2026-09-05)

- 제품 소유자가 실제 게시글 좋아요·취소와 고정 방향 로그인 아이콘의 정상 동작을 확인했다.
- 본문 아래 댓글 영역에 여러 줄 입력칸과 등록 버튼을 추가했다. 비로그인 상태에서는 로그인 시트를
  열고, 답글은 대상 이름·내용 요약을 보여 주며 입력칸으로 이동한다. 삭제된 댓글의 답글 버튼은 숨긴다.
- 기존 Android의 `POST /comment/write` form 계약과 NUBO/GOAPI의 `POST /comment/reply`,
  `replyTargetUid`를 재사용한다. 양끝 공백 제거 후 Android와 같은 UTF-16 길이 10 이상을 적용하며
  서버의 JWT·권한·차단 계정·게시글/댓글 범위 검증은 유지한다. 백엔드 변경·교체는 없다.
- 서버가 양수 댓글 UID를 반환한 뒤 초안을 비우고 피드·상세 댓글 수를 공유한다. 중복 전송을 막고,
  목록 갱신 지연·실패·페이지 추가 중에도 방금 등록한 댓글을 유지하며 서버에서 확인된 UID는 합친다.
  전송 실패 시 초안을 보존하고 결과 유실 시 목록 확인 후 명시적으로 재전송한다. 계정 변경 뒤 늦은
  응답은 무시하며 초안은 현재 상세 화면 메모리에만 둔다.
- 단위 80개에서 form/답글 계약·내용 인코딩·최소 길이·성공/거부/응답 유실·중복 전송·로그아웃 경합·
  목록 갱신 경합과 페이지 합치기를 검증했다. 기존 UI 13개와 신규 댓글 UI 1개가 통과했다. 댓글 로그인→등록→답글→실패 시 초안 보존→
  피드 댓글 수를 검증했다.
- Debug/Release 빌드와 댓글·답글 화면 캡처를 확인했고 Swift `-O` 자동 서명 앱을 연결된 iPhone 17에
  설치·실행했다. 운영 서버에 댓글을 쓰는 제품 QA는 제품 소유자가 수행한다.

## 조건부 입력 안내와 댓글 좋아요 (2026-09-05)

- 제품 소유자가 실제 댓글·답글 등록을 확인했다. `10자 이상 입력해 주세요` 안내는 양끝 공백을 뺀
  UTF-16 길이가 10 미만일 때만 보이며 조건을 충족하면 즉시 사라진다.
- 로그인하면 기존 `GET /comment/list`를 인증해서 다시 읽고 개인별 `liked`를 확인한다. 확인된 상태에서
  Android와 같은 `PATCH /comment/like` JSON 계약으로 좋아요·취소하며, 성공 뒤 하트와 숫자를 바꾼다.
  비로그인 하트는 로그인 시트를 열지만 로그인만으로 좋아요를 자동 전송하지 않는다.
- 중복 탭과 결과가 불명확한 재전송을 막고 재조회로 복구한다. 로그아웃·계정 변경 때 이전 계정의
  개인별 하트 상태를 즉시 지우며 늦게 도착한 응답을 반영하지 않는다. GOAPI·Android 변경과 백엔드
  교체는 필요 없다.
- 단위 84개와 UI 14개를 통과했다. 요청 body·인증 목록·좋아요/취소·중복 탭·응답 유실·로그아웃 경합,
  입력 안내 표시 조건을 검증했고 Debug/Release와 댓글 하트 캡처를 확인했다. Swift `-O` 자동 서명 앱을
  iPhone 17에 설치했으며 기기 잠금으로 자동 실행은 되지 않았다. 다음 작업은 본인 댓글 수정·삭제다.

## 본인 댓글 수정·삭제 (2026-09-05)

- 로그인한 사용자가 쓴 댓글에는 좋아요 옆에 `수정`·`삭제`를 항상 표시하고 각 버튼에 44pt 이상의 터치
  영역을 둔다. 수정은 별도 시트에서 2자 이상을 요구하며 Android와 같은 HTML 문자 이스케이프를 거쳐
  기존 `PATCH /comment/modify` 계약으로 보낸다. 성공한 뒤에만 화면을 갱신하고 실패하면 입력한 내용을
  유지한다.
- 삭제는 확인 화면을 거쳐 기존 `DELETE /comment/remove` 계약을 사용한다. 답글이 있는 원댓글은
  `삭제된 댓글입니다.` 자리를 남겨 대화 흐름과 댓글 수를 보존하고, 답글 또는 답글 없는 댓글은 목록과
  피드·상세 댓글 수에서 제거한다.
- 중복 요청을 막고 삭제 결과가 불명확하면 목록 재조회 전까지 다시 보내지 않는다. 로그아웃·계정 변경 뒤
  늦은 응답도 반영하지 않는다. GOAPI·Android 계약과 서버 배포 변경은 필요 없다.
- 단위 88개와 UI 15개를 통과했다. 댓글 작성→수정→답글→원댓글 자리 보존 삭제→답글 삭제→피드 수치
  반영을 한 흐름으로 검증했고 캡처에서 자리표시자와 답글 배치를 확인했다. Debug/Release 빌드도
  통과했다. 최종 Swift `-O` 자동 서명 Debug 앱을 연결된 iPhone 17에 설치·실행했다.

## Google 로그인 (2026-09-05)

- GoogleSignIn-iOS 9.2.0 공식 SDK와 버튼을 추가했다. iOS client ID로 인증 화면을 열고 Android와 같은
  Web server client ID를 audience로 하는 ID token을 기존 `POST /auth/android/google` form 계약에
  전달한다. 성공한 access/refresh 쌍은 이메일 로그인과 같은 순서로 Keychain에 저장한다.
- client ID는 Git이 무시하는 `Config/Debug.local.xcconfig`와 `Config/Release.local.xcconfig`에서만
  주입한다. 설정이 없으면 Google 버튼을 숨겨 기존 이메일 로그인과 공개 감상을 유지한다. 로그인 취소는
  오류로 표시하지 않으며 Google 공식 버튼의 바깥 터치 영역은 48pt로 확보했다.
- GOAPI의 기존 경로·응답과 Android 호환성은 유지하면서 ID token 검증을 Google 공개키 기반 공식
  validator로 강화했다. audience, 서명·만료와 boolean `email_verified`를 검증하고 차단 계정 정책 및
  기존 이메일 기반 계정 조회는 유지한다. DB migration은 없다.
- iOS 전체 단위 테스트 91개와 이메일 로그인·다크/큰 글자·Google 로그인의 관련 UI 테스트 3개,
  Debug/Release 빌드가 통과했다. GOAPI 전체 test·vet 및 Ubuntu 22.04·24.04, x86-64 호환 Docker 빌드도
  통과했으며 교체용 바이너리 SHA-256은
  `c1653d19ccdce515f3eef5e647e80a9624f911556a35317443bdc2e81dc05788`이다.
- 제품 소유자가 개발·운영 OAuth client를 로컬 설정에 넣고 실제 iPhone에서 Google 로그인이 정상
  동작함을 확인했다. SDK 기본 버튼이 반투명 Form 행 안에 겹쳐 보이던 화면은 Google 공식 다색 G와
  `Google로 로그인` 문구를 사용한 단일 48pt 캡슐 버튼으로 교체했다. 라이트 neutral·다크 브랜드 색과
  iOS용 로고 여백을 적용하고 바깥 Form 행 배경을 제거했다.
- 최종 커스텀 버튼은 전체 단위 91개, Google 로그인 및 라이트·다크·큰 글자 UI 2개를 통과했다. 캡처에서
  내부 사각형이 사라지고 큰 글자에서도 잘리지 않음을 확인했으며 Release build와 정적 분석을 통과했다.
  서명 Debug 앱은 iPhone 17에 설치했고 기기 잠금으로 자동 실행만 되지 않았다. Apple 로그인은 provider
  subject 저장과 명시적 기존 계정 연결 계약을 다음 독립 작업으로 진행한다.

## Apple 로그인과 기존 계정 연결 (2026-09-05)

- 비로그인 계정 시트에 Apple 기본 로그인 버튼을 추가했다. 서버가 발급한 5분짜리 일회성 nonce를
  Apple 요청과 GOAPI 검증에 함께 사용하며 ID token이나 nonce는 로컬 저장소에 남기지 않는다. 로그인
  성공 뒤 access/refresh 쌍은 이메일·Google 로그인과 같은 Keychain 경로로 저장한다.
- GOAPI `f37b088`은 Apple 공개 키의 `kid`와 RS256 서명, issuer, Debug·Release bundle audience,
  만료·발급 시각, nonce와 이메일 검증 상태를 확인한다. 계정 연결은 Apple의 안정적인 provider subject로
  저장하며 같은 이메일만으로 기존 계정을 자동 병합하지 않는다. 기존 사용자는 이메일 또는 Google로
  로그인한 뒤 내 계정에서 Apple ID를 명시적으로 연결한다.
- 계정 시트의 불필요한 환영 문구를 덜고 Apple·Google 버튼은 각각 48pt 전체 폭으로 유지했다. 일반
  크기의 절반 높이 시트에서 이메일 입력란까지 보이며, 다크 모드와 접근성 큰 글자에서는 시스템 버튼
  색상과 전체 높이·스크롤을 따른다. 로그인 후에는 Apple ID 연결 여부를 별도 섹션에서 보여 준다.
- 전체 단위 테스트 95개와 Apple 신규 로그인·기존 계정 연결·Google 로그인·다크/큰 글자 핵심 UI
  테스트 4개가 통과했다. Debug/Release 빌드, 정적 분석과 캡처 검증을 마쳤고 최종 Swift `-O` 자동 서명
  Debug 앱을 연결된 iPhone 17에 설치·실행했다.
- 운영 반영 전 GOAPI 환경에 `OAUTH_APPLE_CLIENT_IDS=me.sensta.ios.debug,me.sensta.ios`를 설정하고
  새 runtime으로 `./bin/goapi install`을 한 번 실행해 OAuth identity·nonce 테이블을 만들어야 한다.
  교체용 바이너리 SHA-256은 `b84075a8f135fb86a310232da3719cd7768c48d0a3d3140bdd28b6db21668025`다.
  실제 Apple ID 로그인·기존 계정 연결·재실행 세션 복원은 운영 반영 뒤 제품 소유자가 확인한다.

## Apple 로그인 실패 안내와 버튼 통일 (2026-09-05)

- 운영 `/auth/apple/nonce` 발급과 `/auth/signup/status`의 `verified_email`, OAuth 가입 허용 상태를
  확인했다. Face ID 뒤 로그인이 끝나지 않는 가장 가능성 높은 원인은 이미 같은 이메일의 Google 계정이
  있어 서버가 안전한 자동 병합 대신 코드 13과 명시적 연결을 요구한 경우다.
- 기존에도 이 안내를 만들었지만 절반 높이 시트의 이메일 Form 아래에 있어 처음 보이는 영역에서 확인하기
  어려웠다. 이제 Apple 인증 오류를 소셜 버튼 바로 아래에 표시하고 VoiceOver에서도 하나의 안내로 읽는다.
  audience·nonce·이메일 검증 실패도 서로 다른 안전한 문구로 안내하며 ID token과 내부 오류는 노출하지
  않는다.
- Apple 기본 버튼과 Debug 대체 버튼을 Google 버튼과 같은 48pt 캡슐 형태로 통일했다. 라이트·다크와
  접근성 큰 글자 캡처에서 두 버튼의 폭·높이·곡률과 안내 위치를 확인했다.
- GOAPI `28d5728`은 Apple 검증 실패의 상세 원인을 서버 로그에 남기고, 앱에는 allowlist로 제한한 원인만
  반환한다. 전체 test·vet와 Ubuntu 22.04·24.04 및 qemu64/max runtime build를 통과했다. 새 runtime의
  `bin/goapi` SHA-256은 `969c67953912d28668110cec897fc80cc4b218c4619c37158a75baabcfe56d61`이며
  migration이나 환경 변수 변경은 없다.
- iOS 전체 단위 테스트 96개와 Apple 로그인·기존 계정 연결 안내·Google 회귀·다크/큰 글자 관련 UI
  테스트 5개가 통과했다. Release simulator build, Debug 정적 분석과 Swift `-O` 서명 실기기 빌드·설치·
  실행도 완료했다.

## iOS 배경화면 API 검토와 이메일 회원가입 (2026-09-06)

- iOS 공개 SDK에는 타사 앱이 시스템 배경화면을 직접 지정하는 API나 공개 share activity가 없다.
  비공개 Photos의 wallpaper 타입은 App Store 공개 API 규정에 맞지 않으므로 첫 피드 롱터치 배경화면
  지정 기능은 구현하지 않았다. 향후 사용자가 원하면 사진 저장과 설정 앱 안내를 별도 기능으로 검토한다.
- 로그인 시트에 `이메일로 회원가입`을 추가했다. 운영 `/auth/signup/status`의 `verified_email`,
  `invite_only`, `disabled` 정책과 메일 설정을 먼저 확인하고 기존 `/auth/signup`, `/auth/verify` form
  계약으로 이메일·이름·비밀번호·초대 코드와 6자리 인증을 처리한다. 운영 서버는 `verified_email`과
  메일 발송 가능 상태임을 읽기 전용 요청으로 확인했다.
- 비밀번호 규칙과 이름·이메일을 전송 전에 검증하고, 약관 동의 행 전체를 누를 수 있게 했다. 인증 메일
  재발송·가입 정보 수정·완료 후 로그인 이메일 채우기를 제공한다. 가입 비밀번호와 인증 코드는 화면
  메모리에만 두고 완료 또는 화면 이탈 때 지운다. 실제 운영 메일 발송과 계정 생성은 제품 소유자 QA로
  남겼다.
- 전체 단위 테스트 99개와 신규 회원가입 UI 1개, 기존 이메일·Google·Apple 로그인 및 다크/큰 글자 UI
  회귀 5개가 통과했다. Release simulator build와 Debug 정적 분석을 통과했고 Swift `-O` 자동 서명
  Debug 앱을 연결된 iPhone 17에 설치·실행했다. GOAPI·Android 변경과 서버 교체는 필요 없다.

## 로그인·회원가입 동작 구분 (2026-09-06)

- 제품 소유자가 이메일 회원가입의 메일 인증과 실제 계정 생성을 포함한 전체 흐름이 정상 동작함을
  실기기에서 확인했다.
- 제품 소유자 실기기 QA에서 이메일 회원가입 바로 위의 기본 Form 로그인 버튼이 구분선과 겹친 것처럼
  보이는 문제를 확인했다. 이메일 입력 행, 로그인 동작, 회원가입 이동을 각각 분리했다.
- 로그인은 다른 인증 버튼과 같은 48pt 캡슐 형태의 파란색 주 동작으로 표시하고, 회원가입은 아래의
  독립된 표준 NavigationLink 행으로 유지한다. 빈 입력으로 비활성화돼도 버튼 형태와 터치 영역은 유지한다.
- 회원가입 전체 흐름, 이메일 로그인 성공·실패, 다크 모드·큰 글자 UI 테스트가 통과했다. 로그인 버튼의
  최소 높이와 너비를 회귀 검사에 추가하고 수정 캡처를 확인했다. Release build와 정적 분석을 통과했고
  최신 Swift `-O` 서명 Debug 앱을 iPhone 17에 설치·실행했다.

## 첫 피드 동작 정리, 알림과 사진 업로드 (2026-09-06)

- 프로필 사진이 없는 피드 작성자에게 기본 아바타를 표시하지 않고 이름을 왼쪽에 맞췄다. 로그인한
  내 계정에 프로필 사진이 없으면 흰색 선형 인물 아이콘을 사용한다. 첫 피드의 조회수는 숨기고 하단
  정보 영역을 위로 옮겨 중앙의 58pt 원형 glass `+` 버튼과 겹치지 않게 했다. 비로그인 상태에서 `+`를
  누르면 로그인 화면으로 연결한다.
- 로그인 상태에서 검색 오른쪽에 알림 버튼을 표시한다. 기존 Android와 같은 `/home/noti/load`와
  `/home/noti/checked` 계약으로 최근 알림·읽음 상태를 공유하며, 미확인 알림은 빨간 점과 1회 흔들림,
  확인한 상태는 선형 종으로 구분한다. 게시글 알림은 해당 상세로 이동하고 전체 읽음도 제공한다.
- 사진 업로드는 운영 서버의 `GET /editor/config?id=photo`에서 게시판·카테고리를 읽고 기존 Android의
  `POST /editor/write` multipart 계약을 사용한다. 최대 9장을 JPEG로 정규화하고 긴 변을 4096px로
  제한하며 정확한 GPS 메타데이터를 제거한다. 본문 body와 변환 이미지는 임시 파일로 처리하고 성공·
  실패·취소 때 정리한다. 401 토큰 회전 뒤에도 같은 파일을 한 번만 재전송한다.
- 전체 단위 테스트 105개, 신규 피드·알림·업로드 UI 흐름, Debug 정적 분석과 Release 기기 빌드를
  통과했다. 서명된 Release 앱을 iPhone 17에 설치·실행했다. 실사진 선택·다중 업로드·운영 알림은 제품
  소유자 실기기 QA로 남겼다.
- GOAPI는 `sensta-ios` 출처도 Sensta 업로드로 인식해 앱 업적과 게시글 출처를 기록한다. 기존 API와
  데이터베이스는 그대로이며 migration·환경 변수 변경은 없다. 전체 test·vet와 공식 Ubuntu 22.04·
  24.04, qemu64/max runtime 검증을 통과했다. 교체용 `dist/nubo-runtime/bin/goapi`의 SHA-256은
  `5a647b3094bdd3358a4724d0f17aad2bd7fab600b2650c777c0ee685c7cda38d`다.
- 세션 마감 조정으로 하단 정보 묶음을 20pt 내려 업로드 버튼과 가깝게 붙이고 첫 피드 등록일을 제거했다.
  탐색은 상단에서 제목 행 우측으로 옮겨 제목과 중심선을 맞췄다. 상단에는 계정과 로그인 시 알림만 남긴다.
  전체 단위 105개와 피드 배치·알림·업로드, 카드 상세 진입, 탐색→검색 결과→상세 UI 흐름을 통과했고
  시뮬레이터 캡처에서 최종 간격과 정렬을 확인했다. Release 앱도 실제 iPhone에 설치·실행했다.

## Android 공통 앱 아이콘 (2026-09-06)

- iOS target에 빠져 있던 `AppIcon` asset과 Debug·Release asset catalog 설정을 추가했다. Android 8+
  adaptive icon의 최신 조리개·빛의 흐름 전경과 `#2A211B` 배경을 그대로 사용하되, iOS가 모서리 mask를
  적용하도록 둥근 모서리를 이미지에 중복해서 넣지 않았다.
- App Store 원본은 1024×1024 sRGB 8-bit RGB의 완전 불투명 PNG로 만들었다. Xcode 27에서 Debug·Release
  simulator, unsigned Release device와 자동 서명 Debug device build가 통과했다. 생성된 앱의
  `CFBundleIconName=AppIcon`과 120px 홈 화면 icon 산출물을 확인하고 iPhone 17에 덮어 설치했다.
  앱 기능·API·Android·GOAPI 변경은 없다.
